import React from 'react';

function SharedWith(props){
    const shared = props.shared;

    return (
        <>
        {/* <h1>Test!</h1> */}
        {/* <h1></h1>
        {shared.map( (name) => {
           return <h2>{name}</h2>
        } )} */}

        </>
    );
};

export default SharedWith;